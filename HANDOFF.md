# UX/UI Handoff — BrainBlooming

เอกสารส่งมอบงานปรับปรุง UX/UI ทั้งเกม (ทุกหน้า) เน้นกลุ่มผู้เล่นสูงอายุ

- **Branch:** `claude/project-structure-8tcdn0`
- **Commit:** `eafc9466436e4ec0506b7e05bff083fea35a49c1` — "Improve UX/UI accessibility and polish across all screens"
- **วันที่:** 2026-09-01

---

## วิธีรันดูผลบนเครื่องตัวเอง

```bash
git clone https://github.com/rennik98/BrainBlooming.git
cd BrainBlooming
git checkout claude/project-structure-8tcdn0
git pull

npm install
npm run dev
```

เปิดลิงก์ที่ terminal โชว์ (ปกติ `http://localhost:5173`)

- `/` → หน้าแรก
- `/map.html` → กระดานเกม (เพิ่ม `?debug` ต่อท้าย URL เพื่อเปิด 🧪 test panel สุ่มการ์ดได้ทุกใบโดยไม่ต้องทอยลูกเต๋า)
- `/cards-pdf.html` → รายการการ์ดพิมพ์ (ไม่ได้แก้ในรอบนี้)

---

## สรุปสิ่งที่แก้ (11 ข้อ) แยกตามหน้า

### หน้าแรก (`index.html`, `css/home.css`, `js/home.js`)
| # | รายการ | รายละเอียด |
|---|--------|-----------|
| 1 | Label ใต้ปุ่มไอคอน | ปุ่ม Settings / Show Cards มีข้อความบอกชัดเจน ไม่ใช่ไอคอนเปล่าๆ, ขยาย touch target |
| 2 | ปุ่ม "วิธีเล่น" | ปุ่มใหม่ 📖 เปิดโมดัลอธิบายกติกา 4 ขั้นตอน (bilingual, สลับ EN/TH ได้) |
| 3 | ปุ่มภาษา EN/TH บนหน้าแรก | ก่อนหน้านี้ต้องเข้า Settings ก่อนถึงจะเปลี่ยนภาษาได้ ตอนนี้มีปุ่มลอยมุมซ้ายบนให้กดตรงๆ |
| 4 | แผงชื่อผู้เล่น | ช่องกรอกชื่อที่เคยลอยแยกจากแผงหลัก ตอนนี้รวมเป็นแผงเดียวกัน มีหัวข้อ "Player Names" ชัดเจน |
| 5 | Checkmark ตอนเลือกจำนวนผู้เล่น | เพิ่ม badge ✓ สีเขียวมุมขวาบนของเหรียญที่เลือก แทนแค่ glow เดิม |

### หน้ากระดานเกม (`map.html`, `css/map.css`, `js/map.js`)
| # | รายการ | รายละเอียด |
|---|--------|-----------|
| 6 | จัดกลุ่มปุ่ม toolbar | แยกกลุ่ม home/เสียง/save ออกจากกลุ่ม EN/TH เว้นระยะห่างและขยายขนาดปุ่มเป็น 40×40px กันกดพลาด |
| 7 | โทเค็นผู้เล่นที่จุดเริ่ม | เพิ่มระยะ offset ระหว่างโทเค็น (`TOKEN_OFFSETS`) ไม่ให้ซ้อนทับกันจนแยกไม่ออกเวลามีผู้เล่นหลายคน |
| 8 | ป้ายคะแนน + Animation | ป้ายคะแนนเด่นขึ้น (พื้นหลังทึบ+ตัวเลขใหญ่ขึ้น) และมี `+N`/`-N` ลอยขึ้นเวลาคะแนนเปลี่ยน (`showScorePop`) |
| 9 | Progress indicator | แถบเล็กๆ เหนือปุ่มทอยลูกเต๋า บอกว่าผู้เล่นปัจจุบันอยู่ช่องที่เท่าไหร่จาก 39 ช่อง |

### การ์ดป็อปอัพ (`css/card.css`)
| # | รายการ | รายละเอียด |
|---|--------|-----------|
| 10 | ฟอนต์คำสั่งใหญ่ขึ้น | `.card-instruction-en/-th` ขยายขนาด + น้ำหนักตัวอักษรเพิ่มเป็น 600 อ่านง่ายขึ้น |
| 11 | คอนทราสต์ปุ่ม Draw | `.card-btn-pass` (ใช้กับ Draw / No Winner) เปลี่ยนจากพื้นโปร่งใสอ่านยาก เป็นพื้นน้ำตาลทึบตัวอักษรขาว |

---

## จุดที่ควรลองทดสอบ

1. เปิดหน้าแรก → ลองกดปุ่ม EN/TH มุมซ้ายบน ดูว่า label ทุกปุ่ม + โมดัล How to Play เปลี่ยนภาษาตามด้วย
2. กด Start → เลือกผู้เล่น 4 คน → ดู checkmark และแผงชื่อผู้เล่นที่รวมเป็นชิ้นเดียว
3. เข้าเกม (`map.html?debug`) → เปิด 🧪 test panel → สุ่มการ์ด reward เพื่อดู `+N pts` ลอยขึ้นที่ป้ายคะแนน
4. ทอยลูกเต๋าจริงดู progress bar ขยับตามตำแหน่ง
5. เปิดการ์ด H2H ดูปุ่ม "Draw" ว่าอ่านง่ายขึ้นจริงไหม

## หมายเหตุ / สิ่งที่ยังไม่ได้ทำ (ถ้าอยากทำต่อ)

- ยังไม่มี "reduce motion" toggle สำหรับ falling petals + floating buttons (priority ต่ำ)
- `cards-pdf.html` ยังไม่ได้ปรับ UX ในรอบนี้
- ยังไม่ได้ทดสอบบนอุปกรณ์ทัชสกรีนจริง (ทดสอบผ่าน headless browser + screenshot เท่านั้น)
